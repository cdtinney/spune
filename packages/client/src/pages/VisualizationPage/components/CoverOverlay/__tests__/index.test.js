import React from 'react';
import { mount } from 'enzyme';
import CoverOverlay from '../';

describe('CoverOverlay', () => {
  it('renders', () => {
    mount(
      <CoverOverlay />
    );
  });
});
